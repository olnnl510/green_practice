<?php // if
    if(0)
    {
        print "안녕하세요.";
    }
    
    else{
        print "안녕히 가세요.";
    }
?>

<!--
    if ()안 : true, flase 에 대한 결과값 밖에 못옴

    ex 숫자 : 0 false, 나머지 true
    ex 문자 : 비어있다 false, 비어있지않다 true
    ex 변수 : 비어있다 false, 비어있지않다 true
    ex 비교연산자

    else : if가 아니면 {} (else 있으면 무조건 둘중 하나 실행) true면 if실행 false면 else 실행. 옵션.

    if else : 그룹이기 때문에 하나만 실행됨 (comparison 과 다름. 다 분리되어있음)

    if, else if, else if, else
    A B C D 중에 하나만 실행됨. (else 가 붙는 순간 무조건 하나 실행됨)

-->